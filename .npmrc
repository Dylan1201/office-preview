# Verdaccio私有仓库配置
registry=http://localhost:4873/

# always-auth对于私有仓库通常需要
//localhost:4873/:_authToken=""

# 发布配置
//localhost:4873/:always-auth=false

# 使用淘宝镜像作为公共包源
@vue:registry=https://registry.npmmirror.com
@types:registry=https://registry.npmmirror.com
